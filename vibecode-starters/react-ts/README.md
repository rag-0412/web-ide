# react-ts starter (placeholder)

This folder is a placeholder to avoid runtime errors when scanning starter templates.
If you intend to ship a real starter, replace the contents with a real React + TypeScript starter project.
